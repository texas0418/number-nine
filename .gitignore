node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
# Anchored: the top-level prebuild-managed projects only. Bare "ios/" also
# matched modules/*/ios, which silently kept the local Expo modules'
# native source out of git (expo-doctor flags this).
/ios
/android
.DS_Store
site/*.zip
